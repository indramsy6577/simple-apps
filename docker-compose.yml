name: simple

services:
  app:
    image: indramuliasy/simple-indra:1.1
    ports:
      - "5000:3000"
    volumes:
      - vol-simple:/app/public/images/
    user: "lab-91"  # Menambahkan user 'lab-91' pada container
volumes:
  vol-simple:
