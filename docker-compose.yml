name: simple

services:
  app:
    image: indramuliasy/simple-indra:1.1
    ports:
      - "5000:3000"
    volumes:
      - vol-simple:/app/public/images/
    user: "app"  # Menambahkan user 'app' pada container
volumes:
  vol-simple:
